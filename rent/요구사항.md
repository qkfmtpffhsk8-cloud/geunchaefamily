# 근채패밀리 월세 매물 웹페이지 — 요구사항 (Claude Code 작업지시서)

## 1. 목적
와이프와 공유하는 웹페이지. 보증금·월세 상한을 입력하면 조건에 맞는 월세 매물을 보여주고,
후보를 골라 나란히 비교할 수 있게 한다. 이사 기한 2026-11-30.

## 2. 대상 지역
1. 과천시 전체
2. 서울시 전 지역 (25개 구)

## 3. 데이터 소스 (수집 스크립트 `collect.py`)
| 소스 | 범위 | 성격 | 비고 |
|---|---|---|---|
| 서울시 열린데이터광장 "부동산 전월세가" (`tbLnOpendataRentV`) | 서울 25개 구, 최근 6개월 | 실거래 (계약된 값) | 서울 전체를 한 API로. 키는 `SEOUL_KEY` |
| 국토부 실거래 API (아파트·오피스텔 전월세) | 과천시(41290), 최근 6개월 | 실거래 | 키는 `MOLIT_KEY` |
| 네이버부동산 (new.land.naver.com) | 과천 전체 + 서울 관심 구 | 현재 매물 (호가) | 관심 구는 `config.json`의 `naver_gu` 목록. 401/403 시 playwright(chromium headless)로 자동 전환 |

- 월세만 수집 (전세 제외). 유형: 아파트, 오피스텔, 빌라/연립.
- 수집 단계에서는 가격·면적 필터 없이 전부 저장. 필터는 웹페이지에서.
- 결과: `docs/data/listings.json` (현재 매물), `docs/data/deals.json` (실거래), `docs/data/meta.json` (수집 시각, 건수).
- 실거래는 최근 3개월만 JSON에 넣어 용량 관리 (원본 CSV는 `data/`에 보관).

레코드 공통 필드:
```
source(현재매물|실거래), region(구/시), dong, complex, type(아파트|오피스텔|빌라),
area_m2, floor, deposit(만원), rent(만원), built_year, date(계약월 or 확인일),
features(특징 텍스트), url(네이버 링크, 실거래는 null), lat, lng(가능하면)
```

## 4. 웹페이지 (`docs/index.html`, 단일 파일 HTML+JS, GitHub Pages)
호스팅: GitHub Pages (`docs/` 폴더). 로그인 없음, 잠자기 없음, 링크 하나로 공유.

### 상단 — 내 조건 (모바일 우선)
- 보증금 최대 (만원), 월세 최대 (만원) — 숫자 입력 + 프리셋 버튼 (5천/1억/2억/3억, 100/150/200/300)
- 지역: 과천 / 서울 전체 / 구 선택(다중)
- 유형: 아파트 / 오피스텔 / 빌라 (다중)
- 전용면적 범위 (㎡)
- 출처: 현재매물 / 실거래
- 전월세전환율 (기본 5.5%) → "환산 총주거비(월)" 계산에 사용
- 조건은 URL 쿼리에 저장 → 링크를 그대로 와이프에게 보내면 같은 조건이 열림

### 결과 목록
- 카드형 리스트. 카드에: 단지명·동, 유형, 전용㎡, 층, 보증금/월세, 환산총주거비, 건축년도, 특징, 네이버 링크 버튼
- 정렬: 환산총주거비 / 월세 / 보증금 / 면적 / 최신
- 상단 요약: 건수, 월세 중앙값, 보증금 중앙값, 구별 건수
- 각 카드에 ⭐ 즐겨찾기, ➕ 비교 담기 (localStorage 저장 — GitHub Pages라 사용 가능)

### 비교 화면
- 담은 매물(최대 5개)을 가로 스크롤 표로 나란히: 보증금, 월세, 환산총주거비, 3년 총액, 전용㎡, 층, 건축년도, 지역, 링크
- 메모 칸 (localStorage)
- "링크 복사" → 비교 대상 ID를 URL에 담아 공유

### 참고 지표
- 같은 단지의 실거래(최근 3개월) 중앙값을 현재 매물 카드에 함께 표시 → 호가가 실거래보다 얼마나 높은지

## 5. 자동화
- Claude Code 웹 routine: 매일 06:00 KST `collect.py` 실행 → `docs/data/*.json` 갱신 → main에 커밋·푸시
- GitHub Pages는 main의 `docs/`를 자동 배포

## 6. 환경변수 (Claude Code 웹 환경에 등록)
```
SEOUL_KEY=서울열린데이터광장 인증키
MOLIT_KEY=공공데이터포털 Decoding 키
TZ=Asia/Seoul
```
허용 도메인: `new.land.naver.com`, `*.naver.com`, `*.pstatic.net`, `openapi.seoul.go.kr`, `apis.data.go.kr`, `*.data.go.kr`

## 7. 완료 기준
- [ ] `python collect.py` 실행 시 과천+서울 JSON 생성, 건수 출력
- [ ] GitHub Pages URL에서 폰으로 열어 보증금 1억/월세 150 입력 → 결과 표시
- [ ] 매물 3개 담아 비교 화면 확인
- [ ] 조건 포함 URL을 복사해 다른 폰에서 열면 동일 조건 표시
- [ ] routine이 매일 실행되어 meta.json의 수집 시각이 갱신됨
